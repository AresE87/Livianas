---
interface Props {
  text: string;
}

const { text } = Astro.props;
---

<span class="block font-bold text-xs uppercase tracking-[0.15em] text-[var(--color-sage)] mb-3 font-[var(--font-body)]">
  {text}
</span>
